version: "3"

services:
  web:
    image: nextcloud-alpine
    volumes:
      - ./data:/usr/share/webapps/nextcloud/data
      - ./config:/usr/share/webapps/nextcloud/config
    ports:
      - "8080:8080"
